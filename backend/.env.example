# Server Configuration
PORT=3001
NODE_ENV=production
BASE_URL=https://your-domain.com
ALLOWED_ORIGINS=https://your-frontend.com

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/flexanon

# Solana Configuration
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_PROGRAM_ID=your_program_id_here
SOLANA_NETWORK=devnet

# Zerion API
ZERION_API_KEY=your_zerion_api_key_here

# Relayer Configuration (Optional - for privacy-preserving commits)
# Option 1: Use base58 encoded private key
RELAYER_PRIVATE_KEY=your_base58_private_key

# Option 2: Use keypair file path
# RELAYER_KEYPAIR_PATH=/path/to/relayer-keypair.json

# If neither is set, defaults to ~/.config/solana/id.json
